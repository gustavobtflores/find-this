import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
	palette: {
		primary: {
			main: '#6200ee',
		},
		secondary: {
			main: '#ffffff',
		},
	},
	font: {
		regular: 'Roboto',
	},
});

export default theme;
