import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#6200ee",
    },
    secondary: {
      main: "#ffffff",
    },
    text: "#000000B3",
    background: "#00000014",
  },
  font: {
    regular: "Roboto",
  },
});

export default theme;
